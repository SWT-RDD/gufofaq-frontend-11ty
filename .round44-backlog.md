# round44 未收的發現（29 路稽核，round44a/b 已收的不列）

> **round45 進行中**：八路修復 agent 已派出（3-1-x／3-2·3-4·3-5·3-6／5-2·5-4·5-10／5-5·5-6·5-7／
> qaTest·qaHistory·5-8·5-9／dataImport＋元件／元件契約＋ui scss／測試網／i18n），彼此檔案不重疊、全部禁跑 build。
> 六條「需要裁決」已由我裁掉並寫進 GUIDELINE（見下方 H 節每條的「裁決」）；表格排序那條另立 `ui/table-sort`。
> 我仍欠：`.sort` 的 aria-sort 視覺（要等元件契約 agent 放開 `_default-table.scss`）、table-sort 的
> 單元測試（要等測試 agent 放開 `guideline.test.mjs`）、各 agent 回報的 en.json key 與元件庫頁補件。

> 每條都由 agent 回報並附了出處，但**我尚未自己逐條複驗**。動手前一律先自己 grep／讀正本
> （§3-2；本專案的教訓是 subagent 報告會有錯，round44 已駁回過數條）。

## A. 逐 raise 對不齊（§5）— 同一族，建議一次收
- 3-1-1 刪除資料集 `deleteToast` 缺 `ChatConfigRejectedError` → 全域 handler 的 400（warning，
  修法是去對話設定頁改那一欄），現況折進紅色終局。
- 3-1-3 `editSaveToast`（資料集名稱那顆）缺 403 `dataset.not_writable_group`，而同頁另兩顆
  editable-block 把它列成 warning ＝同一道守衛同頁兩種語意型別。
- 3-1-3 delete-modal 的 403 折進 error，同上。
- 5-10 刪維度／刪代碼缺 409 `tag_filter_is_on`（示範資料 `mode: "on"` 正好在觸發態）；
  新增代碼缺 `code_is_reserved`，且把 `code_exists` 與 `code_indistinguishable` 折成一句。
- 5-4 三顆鈕缺 400 `stats.classifier_not_configured`。
- 5-6-2 建立／儲存缺 400 `mcp.env_key_blocked`（`_assert_env_keys_are_deliverable`，
  不走 `_proxy_write` 故沒有 code，會被折進「請稍後再試」而重試永遠不會成功）。
- 5-8 產生金鑰缺 400 `credential.origins_required`。
- 4-1 下載 CSV：`_validate_export_range` 有六個 400 出口，檔頭只寫三道、toast 只列兩段。
- 2-2-4「開始執行」缺 `bad_override_values`(400 warning) 與 `profile_out_of_overridable_range`(502)。
- 3-5 五支處置＋還原缺資源層 scope 的 403（`_assert_writable`／`can_write_dataset`）。
- 3-2/3-4 建立鈕的 warning 段缺長度與格式（見 B）。

## B. 上限／格式約束沒有落點（§3-2 上限寫在消費點、§4 aria-describedby）
- 3-2 術語表四欄零 `maxlength`、零可見上限、零 `aria-describedby`
  （GufoRAG `glossary.py` 的 MAX_TABLE_NAME_LEN=64／WHEN_TO_CONSULT=1024／TOOL_PROMPT=1024，
  product `_MAX_NORMALIZE_REGEX_LEN=500`，另有 `contains_markup` 不可含標籤）。
  regex 的格式約束只寫在 placeholder（一打字就消失），逐列編輯那顆連 placeholder 都沒有。
- 3-4 Skill 名稱格式（`^[a-z0-9]+(-[a-z0-9]+)*$`）與兩欄長度（64／1024）畫面上一句都沒有。
- 2-2-4 `#runPromptInput` 缺 maxlength=20000 ＋ hint；`#runSampleTotal` 缺可見區間 1–100。
- prompt-edit 注入的 textarea 缺 maxlength=20000 ＋ hint（正典在 skill-editor-modal:61）。
- skill-editor-modal 參數格式欄缺 maxlength=4096（`MAX_SKILL_INPUT_SCHEMA_LEN`）。
- builtin-tool-card 兩欄缺「不得含 XML/HTML 標籤」（`_XML_TAG_RE`）。
- 3-6 逐列就地編輯的兩欄只有 maxlength、沒有可見上限（新增區有）。
- 5-2 政策主題／出口替換缺「N / 30」；詞彙過濾 hint 缺 200/200。
- data-time-filter 起訖欄缺區間限制（四類 400 全在 `_resolve_range`）。
- manage-tenant-modal 使用期「延展天數與到期日擇一」只寫在 toast。

## C. 示範資料不自洽（§6）
- manage-members-modal 勾選態 {admin, editor} 對不上 5-5-2 任一群組（應為 editor+viewer）。
- 5-7 `login.failed` 那列 `tenantId: 31` 配 operator「—」（上游同一個三元式，兩欄同生同滅）；
  `target: "—"` 但該 action 無條件帶 target；`ask` 那列編了 `chat_log #4821`（ask 不帶 target）。
- 5-6-1 `platformAdminRows` 列序（`platform_role.desc()` ⇒ auditor 在前）；
  5-6-2 列序（`created_at.desc()` ⇒ 3→2→1）。
- 5-10 `有效期` 130/132 與缺欄檔 8 檔互斥（未標註份數 ≥ 8）。
- 3-5 #940 non_question 要求資料集是 qa、#901 的 merge 要求同一個資料集是 file。
- 3-5 `superseded_by_newer` 不存在（正本是 `superseded_by_newer_as_of`），且該 evidence 形狀
  與 `valid_until` 互斥；`reasons`／`missing_slots` 上游是 list、畫成純量。
- 1-2-1 檔名用 `{14位時間戳}_{原檔名}`（正本是 `{14}_{6hex}_{序號}_`，1-1-6 檔頭自己講過）；
  success-box 內建預設同一個壞形狀（元件庫頁真的畫得出來）。
- 1-2-1 匯入失敗訊息「萃取額度已用罄」屬於另一支端點（extract.py），批次匯入不碰萃取額度。
- 1-2-1 續表計數 1 但具名清單沒有續表那一筆；兩份清單的 `〔表格 N〕` 定位記號被改寫掉。
- 1-1-4 internal_note 的 `extraOption: "維護人"` 是 1-1-3 預覽表沒有的第 5 欄。
- 3-1-1 逐列刪除用靜態 `deleteTargetName`（三列都問第一筆）；應改 `deleteTargetId`。
- 3-1-3 `record_count` 可為 null（上游明寫前端顯示「—」），切版沒有這一態。
- 2-2-3 兩側 textarea 渲染出逐字相同的提示詞，卻標著 V3／V2；且與 compare modal 的 B 側不同。
- 2-2-5 `unchanged` 桶 count:8/rows:2 觸發截斷句，但上限釘 20 後 React 端不可能觸發。
- 5-2 逐代碼覆寫用 `tariff`／`procedure`，本示範租戶的 note3 代碼是 `B06`／`B02`。
- 5-2 `policy-lookup` 是停用中的 skill（3-4 `enabled:false`）卻沒有字面標示。
- select-dataset-modal 預選「資料集AA」而 1-1-1 的模擬 select 同時顯示「請選擇」。
- record-identity `titleSource=="filename"` 時標題與檔名逐字相同、印兩次。

## D. 逆向 API 缺口（§3-2）
- 5-6-1 平台權限表無列鍵（`PlatformAdminOut.id`）；manage-tenant-modal 成員列無列鍵、
  `platform_role` 無落點（而它正是代理登入那道 403 的判準）。
- `GET /platform/license-usage` 五欄全站零落點；`TenantUsageOut.storage_used_mb` 無落點；
  `DisclaimerVersionOut.created_by` 無落點（5-6-3 為同一理由補了核發者／撤銷者）。
- 1-1-6 `label_sync_warning`（200 態警語）全站零落點；`health_scan` 同。
- 2-2-4 `list_suites` 的 limit／offset／total_count 三樣全無落點（docstring 明寫
  「分頁沒有總數等於沒有分頁」）。
- 3-1-2／3-1-3 切塊三參數（chunk_size／chunk_overlap／prepend_title_each_chunk）全站零落點，
  且 `_assert_chunk_params_still_settable` 是一道 409 硬閘門。
- 3-1-5 只列內層 `ChunkOut`、漏掉外層 `FileChunksOut` 五欄（畫出來的「類型」正是被漏掉的那一個）。
- 3-5 `ScanIn.dataset_id`／`days`／`top_n` 無控制項（而可見文案叫使用者「把窗拉長」）。
- 3-6 `aliasCount` 上游根本不回（React 已寫成 `alias_count?` ＋ `?? "—"`）。
- 5-7 `id` 欄無落點也沒寫理由；5-9 `api_key_hint`／`has_key` 無落點。
- untagged-files-modal `doc_name` 可為 null、`document_id` 無落點。

## E. a11y（§4）
- 5-5-1／5-5-2 逐列「所屬群組／能力」checkbox 群沒有列脈絡（同頁 4～28 顆同名）。
- manage-tenant-modal 成員列四顆同名鈕（非 `<tr>`，無列脈絡）。
- 5-2 「主回答」`role="group"` 框住整個生成面板 14 顆控制項（含決定別組的 switch）。
- 5-2 輸出規則參數欄三顆「上限」＋兩顆「清單（一行一個）」同名。
- header `menuItems` 兩顆「歷史紀錄」（手機選單可同時展開兩個子選單）。
- 3-1-6 13 顆排序鈕全站沒有行為（`.sort` 有樣式故不是 hook）；4-1 同型 1 顆。→ 需裁決：
  寫一支 `ui/table-sort`，或把「表格排序」寫進 §5「不在切版範圍的互動」。
- 3-1-6 `#cmpSortWord` 住在 `#panelCompare` 內，卻被 `#panelRaw` 的 7 顆鈕引用。
- 3-1-3 逐列 checkbox 的 `aria-labelledby` 順序顛倒（全站其餘 10 個實例都是「列名 → 表頭」）。
- qa-side-panel 收合是位移，內容仍留在 tab 序（`aria-expanded="false"` 與「還 tab 得進去」矛盾）。
- 3-1-6／1-1-3 等 collapse-toggle 逐**格**重複同名（每列 6–7 顆「展開」）。

## F. 契約／文件
- ui/chat-message 契約說「三個消費點逐字相同」，實際 `.message-icon` 三份都不同、
  chip 的 `data-capability` 前台不帶、widget 根本沒有這一層。→ 應拆成 ③／③′ 兩型。
- ab-compare 契約把 `.message-icon` 5 顆鈕砍成 1 顆卻寫「一個都不能省」。
- chart-shell 契約從 `.chart-box` 起筆，漏掉讓 `flex: 1 1 0%` 成立的 `.flex-row` 祖先；
  契約 ① 用 `{{ cbId }}` 但沒帶定義它的 `{% set %}`。
- apply-settings-compare-modal 漏了成對 hook `.js-apply-current-title`（左欄），
  saas 已同形複製；type-to-confirm 片語被翻譯（繁中打「刪除」、英文打 DELETE）。
- editable-block 的 `{% if editId %}…{% else %}` 是死分支，且真觸發會吐 `for=""`。
- tests PAIRS 的 `editTenantRole` 是幽靈參數（元件從不吐 `data-tenant-role`）→ 假綠。
- qa-record-tabs 的 `data-record-index` 三件都對不上（真 app 是 `data-chat-sn`、值是 id 不是
  loop.index、saas 根本不用這顆屬性）。
- import-report 的 `droppedLinks.count` 烤字面量（上游定義上等於陣列長度）。
- 5-10 `tagSlotKeys` 另烤一份 12 個字面量（正本是 `ui/field-slot-catalog` ＋ 角色過濾）。
- 5-3 三句話指向不存在的變數 `attributionSlots`；歸因槽是「五類角色、22 槽」不是「四類、21」。
- _step-nodes.scss／_pagination.scss／_storage-bar.scss 的「逐位元照抄／verbatim」與事實不符
  （刪了 box-sizing、改了色 token、行號範圍多吃 52 行）。
- 5-6-1／5-6-2 的 delete-modal 未給 `deleteConfirmClass`／`deleteConfirmId`（全站 9–10 頁同形）
  → 需裁決：整批補，或改寫 delete-modal 檔頭承認 `deleteTargetId` 是可定址契約。

## G. 測試層（來自假綠分析）
- `distDoc()` 是 20+ 條測試的共用母體，沒有「剝完還剩多少標籤」的守門。
- 九處 class/style/屬性收集器只認雙引號（round42 只修了 `.hidden` 那一處）。
- `§2 set 重設` 的四支解析器不吃 `{%-`（同檔的 `setName` 已經吃了，沒同步）。
- `NAMED`（按鈕主人那條測試自己的常數）與 `NAMED_HOOKS` 是同一概念的兩份清單（29 vs 56），且無 stale 守門。
- `SHOWCASE` 豁免有五份互不相干的清單。
- `cssClasses` 把 `url(...png)` 的副檔名收成 class（`class="png"` 會被判有主人）。
- `FROZEN_BASE` 的 `uploadFileExcel.js` 全 repo 零引用＝死豁免，且該白名單無 stale 守門。
- 平台導覽入口那條的檔名逃逸 regex 寫壞（字元類提早收掉）。
- `builtinToolCards === 14` 把跨 repo 常數的種類數抄進測試四次。
- GUIDELINE 自述「沒有網」的條文，agent 判定**今天可測**的：同頁一套 data-target、
  前後綴空白的補集、`.hidden` 判準①的另一半（src 有引用但 dist 渲染不出的 key）、
  Eleventy 巢狀 include 陷阱、漸層兩端點對比、`:focus-within` 黑名單、`<form>` 白名單、
  頁級 script 兩支、`btn-close-modals` × warning、`sm-gap-*` 必須同掛 `mobile-column`、
  `.click()` 白名單、孤兒 hint id、契約雙向頁面清單、出處要含 repo 名、空 `<option>`。

## H. 需要裁決（規則本身有兩種讀法，不是單純的 bug）
1. §5「每個分支結果都要看得到」前後半句互斥：五條空狀態文案（`dataImport.noUploadedFiles`／
   `dataset.noFiles`／`health.uncoveredNone`／`health.uncoveredNoReason`／`serviceKey.none`）
   在 dist 永遠渲染不出來。照前半句是五個缺口，照後半句是正典。
2. `data-capability` 不足時的渲染規則缺席 → 全站對同一道 403 分成兩派（4 處列了、20+ 處沒列）。
3. `data-capability` 的宣告單位是控制項還是區塊（5-2 用了 8 顆 `.tab-content`，規範只授權
   `data-platform-role` 走區塊）。
4. 表格排序歸哪一桶（見 E）。
5. 租戶自取的顯示名是資料還是 chrome（3-1-5 掛 `data-i18n`、3-1-6 當資料，兩頁相反）。
6. `.table-container` 這種「設計系統的純語意包裝層」在 §4 三種死法裡沒有名字。

---

## I. i18n 橫切（最後一支 agent，round44e 已收兩條，其餘待複驗）
- **同文異 key 且英譯逐字相同**：`dataImport.uploadExcel`/`catalog.linkUploadExcel` 都是 "Upload Excel"；
  `dataImport.uploadPdf`/`catalog.linkUploadPdf` 都是 "Upload PDF"。分 key 的前提（英譯必須區別）不成立。
  現有測試看不到：src 側是插值 key（`{{ card.titleKey }}`）屬性正則打不到，dist 側那一輪只收
  `data-i18n` 的文字節點、不收 `data-i18n-<attr>` 的屬性值。
- **glossary-entries-modal 內「詞條」同時譯成 Term 與 Entries**（`toast.saveGlossaryEntries`
  一句話裡就同時出現）。姊妹頁 3-6 已全用 entry。
- **5-2 輸出規則的提示英譯指向兩個不存在的選項名**：譯文寫 "Classified"/"Unclassified"，
  下拉裡只有 Is set / Is not set；同句前綴又把選項引成小寫。
- **引號兩套並存**：彎引號 60 顆 vs 直引號 13 顆（其中 12 顆純違規）；撇號 10 彎 vs 27 直。
  最刺眼：`copyConfigModalTitlePrefix/Suffix` 把「」譯成兩顆一模一樣的直引號。
- **「X 管理」枚舉 8 顆裡 7 顆 X Management**，只有 `nav.widgetTokens` 是 "Embed Keys"，
  而它與 User/Group Management 同在「設定」那張下拉卡裡。
- **`settings.capHistory` 繁中「歷史紀錄」缺動詞**（同組另四顆都是「X 檢視」），
  而測試白名單已逐字登記它 ＝ 白名單掩蓋了繁中那一側的構詞破口。
- **屬性型譯文例外的條件①不成立**（round44 新條）：4-1 的 31 日／一年、5-3 與 5-4 的 365 天／一年
  只活在 `data-toast` 譯文裡，頁上沒有任何常駐可見的資料節點承載同一個約束（條件②三頁都做到了）。
- **三段可見譯文把常數烤進去**（不是屬性、不適用例外）：3-4 的 `skillMaxIterationsHint`（1-20）、
  `skillNameHint`（1-64）、`skillNestedHint`（3 層）——min/max/maxlength 就在同一行。
- **元件庫頁 showcase 自寫的示範觸發器裡，唯一掛 `data-i18n` 的那一顆**（英文模式那一排會
  夾一顆 "Edit entries"）；同頁 demoScoreFloor 的 label 同型但次要。
- **qa-record-tabs 三顆頁籤整組沒有 i18n，而同元件的空狀態有**；且「問答紀錄1」把 chrome 與資料
  烤成一個字面量。
- 次要：`dataset.recordsUnit`「筆」vs `qa.records`「筆資料」英譯都是 "records"（白名單保護不到）；
  `dataImport.droppedLinksPrefix`「已移除」vs `toast.removePendingFile`「移除成功」都是 "Removed"；
  "Conversation Settings" vs "Conversation settings" 五處分岔。
- 疑似（未驗上游）：`comp.uploadDescXlsx`(10MB) 與 `dataImport.uploadDesc*`(30MB) 把上傳大小上限
  烤進可見譯文，且**沒有任何檔頭指名上游符號**，連「改值時 grep 得到全部抄本」都做不到。

### i18n agent 掃過、明確零發現（避免重掃）
同 key 同繁中 1620 顆零分歧（含屬性型，負控驗過巢狀節點抓得到）／`data-i18n-<attr>` 後綴 vs 實際屬性
零不符／264 顆 `data-toast` 三方段數全對位／逐子句雙向一致性零發現／1665 顆 key 字母序零錯位／
5 顆空字串譯文全是刻意省略／27 顆全形候選逐顆合法／資料槽 key 全部成對／分隔符全走 key／
反向孤兒零／冒號折進 key 的 5 顆判定正確／後綴黏連 20 個候選零違規／枚舉成員無部分現身。
